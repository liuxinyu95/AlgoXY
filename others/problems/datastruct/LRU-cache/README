Implement a Least Recently Used (LRU) cache.
It should support constant O(1), time get/set operation.
For any given positive capacity, it drops the least recently used record if reaches the capacity before insertion.